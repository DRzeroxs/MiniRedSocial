@using MiniRedSocial.Core.Application.ViewModels.Hilo;
@model SaveHiloViewModel
@{
    ViewData["Title"] = "Respuestas";
}

<div class="container-fluid">

    <div class="row">
        <div class="col-6 offset-3">
            <div class="card">
                <div class="card-header bg-dark text-light">
                    <h4>Agregar Respuesta</h4>
                </div>
                <div class="card-body">
                    <form method="post" asp-controller="Hilo" asp-action="AgregarRespuesta">

                        <input hidden asp-for="MessageId" />

                        <input hidden asp-for="Id" />

                        <div class="mb-3">
                            <label asp-for="Content" class="form-label">Mensaje: </label>
                            <textarea asp-for="Content" class="form-control"></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>

                        <button type="submit" class="btn btn-primary">Responder</button>
                        <a asp-controller="Home" asp-action="Index" class="btn btn-warning">Volver atras</a>

                    </form>


                </div>

            </div>
        </div>
    </div>

</div>


